import styled from 'styled-components';

export const EductationStyles = styled.div`
  .schoolDiv {
    padding: 15px;
    border: 1px solid var(--lightGray);
    box-shadow: var(--bs);
    background-color: #fff;
    margin-bottom: 20px;
    border-radius: 5px;

    h4,
    h5 {
      margin-bottom: 0;
    }

    p {
      margin-top: 0;
    }

    img {
      display: inline-block;
      width: 40px;
      transform: translateY(15px);
    }
  }
`;
